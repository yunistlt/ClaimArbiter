services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Данные хранятся в /app/data — не стираются при пересборке/рестарте контейнера
      - bot_data:/app/data

volumes:
  bot_data:
